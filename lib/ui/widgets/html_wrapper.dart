import 'package:flutter/material.dart';

class HtmlWrapper extends StatelessWidget {
  final String data;

  const HtmlWrapper(this.data, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
  return Container();
  }
}
