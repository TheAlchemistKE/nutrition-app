import 'package:flutter/material.dart';

class RecommendationProgressBodyYear extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [],
      ),
    );
  }
}
