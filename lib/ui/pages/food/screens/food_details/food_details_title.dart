import 'package:flutter/material.dart';

class FoodDetailsTitle extends StatelessWidget {
  final String name;
  final String category;

  const FoodDetailsTitle({Key? key, required this.name, required this.category})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
